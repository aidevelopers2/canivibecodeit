# secrets — never committed, ever
.env
.env.*
!.env.example
HANDOFF.md
*HANDOFF*

# private business planning — never committed (public repo)
planning/

# private user data (also lives outside the repo tree on the server)
data/private/
*.db
*.sqlite
*.sqlite3
*.log
*.log.*
logs/

# deps & build output
node_modules/
dist/
.astro/

# editor / OS junk
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# heavy AI direction boards live outside the repo (brand/prompts.md documents them)
brand/direction-board*.png
